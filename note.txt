Xóa bộ nhớ đệm khi bị lỗi nhập số -> nhập chữ
scanner.nextLine();

import <ten_package>.*; // sử dụng tất cả class trong package

Calendar now = Calendar.getInstance();
        System.out.println(now.get(Calendar.YEAR)); // Get year now
---------------------------------
note char
Character.isDigit.(str.charAt(i))

Integer.parseInt(String.valueOf(str.charAt(i)));
---------------------
Arrays.stream
-------------------------
updateAcc(List<Account> list, String)